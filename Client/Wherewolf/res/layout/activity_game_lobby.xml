<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="edu.utexas.chatila.wherewolf.activities.GameLobby" 
    android:background = "@drawable/purplebg">

    <ListView
        android:id="@+id/player_lobby_list"
        android:layout_width="match_parent"
        android:layout_height="200dp"
        android:layout_above="@+id/StartGameButton"
        android:layout_alignParentRight="true"
        android:layout_alignParentTop="true"
        android:dividerHeight="10dp"
        android:listSelector="@android:color/black"
        android:minHeight="20dp" >

    </ListView>

    <Button
        android:id="@+id/StartGameButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/player_lobby_list"
        android:layout_alignParentBottom="true"
        android:layout_toLeftOf="@+id/LeaveGameButton"
        android:text="@string/StartGame" />

    <Button
        android:id="@+id/LeaveGameButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@+id/StartGameButton"
        android:layout_alignRight="@+id/player_lobby_list"
        android:text="@string/LeaveGame" />

</RelativeLayout>
